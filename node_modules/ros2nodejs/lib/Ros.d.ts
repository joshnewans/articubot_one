/// <reference types="node" />
import { EventEmitter } from 'events';
import WebSocket from 'ws';
export declare class Ros extends EventEmitter {
    url: string;
    ws?: WebSocket;
    constructor(url: string);
    open(): void;
    close(): void;
    private onOpen;
    private onClose;
    private onMessage;
    send(message: any): void;
    getRandomUUID(): string;
}
